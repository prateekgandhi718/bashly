const BashIdPage = () => {
  return (
    <div>Bash ID Page. On this page we will show the event's start and end date. We can build a form and save the data and then display the data. But the side nav mobile is in chat header so make sure to have that here.</div>
  )
}

export default BashIdPage